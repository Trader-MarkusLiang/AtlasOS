# Capital Relay

Authority: framework definition.

This file defines the capital relay model. Current market placement belongs in
`04_Current_State/AI_Capital_Map_v1.md`.

Version semantics: repository releases use `v0.x Alpha`; judgment labels such as `v0.1 judgment`
identify the knowledge snapshot that first recorded the relay view. Updating the repository version
does not automatically rewrite the historical judgment label.

Current capital migration path:

```text
Compute / GPU
 ↓
Memory / HBM / DRAM
 ↓
PCB / CCL / Copper Foil
 ↓
Equipment
 ↓
Materials
 ↓
Bandwidth
 ↓
Power
 ↓
Workflow
 ↓
Industry AI
```

Current v0.1 judgment:

> Atlas is currently in the stage where Memory is strengthening, Equipment is taking the relay,
> Materials are starting, and Bandwidth is being upgraded into a first-level theme.
